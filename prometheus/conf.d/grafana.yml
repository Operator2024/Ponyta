scrape_configs:
  - job_name: 'grafana'
    scheme: http
    scrape_timeout: 5s
    basic_auth:
      username: admin
      password_file: /etc/prometheus/secret.txt

    static_configs:
      - targets: ['grafana:3000']
        labels:
          status: "production"
          type: "internal"
